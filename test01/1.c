#include <stdio.h>
#include "max.h"

int main()
{
    // 定义一个整数变量a
    int a;
    
    // 提示用户输入两个整数
    printf("请输入两个整数: ");
    
    // 接收用户输入的一个整数并存储到变量a中
    scanf("%d", &a);
    
    // 输出用户输入的整数
    printf("%d\n",a);
    
    // 以下代码被注释掉，原因是可能这部分代码是预留的、未完成的或不再需要的
    // 调用未定义的Max函数比较10和20的大小，并将结果赋值给a
    // int a=Max(10,20);
    
    // 定义一个整数变量b，此处未使用
    // int b;
    
    // 输出Hello World，此行代码未执行因为被注释掉
    // printf("Hello World\n");
    
    // 输出两个整数的最大值，此行代码未执行因为被注释掉
    // printf("Max(1,2)=%d\n", a);
    
    // 输出两个整数的最大值，此行代码未执行因为被注释掉
    // printf("Max(10,20)=%d\n", Max(10,20));
    
    // 程序正常结束，返回0
    return 0;
}